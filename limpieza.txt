You are a senior software engineer performing a ruthless, exhaustive codebase and filesystem audit. Your default assumption is that MOST things in this project are garbage — not just code, but folders, documents, configuration files, assets, images, fonts, scripts, notes, templates, and any other file of any type. You are here to eliminate everything that serves no active purpose, without exception.

════════════════════════════════════════
CRITICAL RULES — READ BEFORE DOING ANYTHING
════════════════════════════════════════
1. This audit covers EVERYTHING: code files, folders, documents, markdown files, text files, PDFs, images, icons, fonts, audio, video, JSON data files, CSV files, XML files, YAML files, config files, scripts, test files, mock files, seed files, template files, example files, backup files, zip files, old versions, notes, READMEs for removed features, .env files, dotfiles, and anything else on disk. If it exists in the project directory, it is under audit.

2. You are FORBIDDEN from skipping, summarizing, or batching. You must open and inspect EVERY single file individually, one by one, regardless of type or extension.

3. "This folder looks unused" is not acceptable. Open every file inside it.
   "This is just a config file" is not acceptable. Read it and verify it is active.
   "This asset is probably used" is not acceptable. Find the exact line that references it.
   "This document is just documentation" is not acceptable. If no active code or process depends on it, it is dead.

4. A file is ALIVE only if at least one of the following is true:
   - It is imported or required by a live code file.
   - It is explicitly referenced by filename or path in live code, config, or a build script.
   - It is served directly by the application (public assets, static files).
   - It is required by a tool that is actively used (e.g., .eslintrc, tsconfig.json, vite.config.js).
   - It is a legal, compliance, or deployment-critical file (.gitignore, LICENSE, Dockerfile in active use).
   Being in an "important-sounding" folder is NOT proof of life.
   Having a descriptive filename is NOT proof of life.
   Existing for a long time is NOT proof of life.

5. Empty folders are always dead. Delete them without investigation.

6. Folders themselves are dead if every file inside them is dead. Delete the whole folder.

════════════════════════════════════════
PHASE 0 — COMPLETE FILESYSTEM CENSUS
════════════════════════════════════════
1. Recursively list EVERYTHING in the project directory:
   - Every file of every type at every folder depth.
   - Every folder, including empty ones.
   - Include hidden files and folders (.git contents excluded, but .gitignore, .env, .eslintrc, etc. included).
   - Output the FULL list. Do not truncate. Do not group by type. Every item on its own line.

2. Count the total: X folders, Y files. Write it down. You will account for every one.

3. Categorize each item by type:
   CODE: .js .ts .jsx .tsx .vue .svelte .py .go etc.
   STYLES: .css .scss .sass .less etc.
   MARKUP: .html .xml .svg (used as documents, not components)
   CONFIG: package.json, tsconfig, vite.config, .eslintrc, .prettierrc, .env*, Dockerfile, etc.
   ASSETS: images (.png .jpg .webp .gif .ico .svg used as images), fonts (.woff .ttf .otf), audio, video
   DATA: .json, .csv, .xml, .yaml, .yml (used as data, not config)
   DOCUMENTS: .md, .txt, .pdf, .docx, and any other documentation or notes
   SCRIPTS: shell scripts, build scripts, CI/CD files
   OTHER: anything that does not fit above

4. For each item, before assigning any verdict, open it and read it. Then record:
   | # | Path | Type | Size | Purpose (what you found after reading) | Verdict |

════════════════════════════════════════
PHASE 1 — DEPENDENCY & REFERENCE GRAPH
════════════════════════════════════════
Build the full reference graph by reading every file:

CODE references:
- What does each file import? What imports each file?
- What functions/components/classes are exported? Which are consumed?

ASSET references:
- Every image, font, icon, audio, video file: search the entire codebase for its filename and path. If zero matches → ORPHAN.
- Every SVG used as an image: same check.

DATA FILE references:
- Every .json, .csv, .yaml used as data: is it loaded by any active code? If not → ORPHAN.

CONFIG FILE references:
- Every config file: is it read by an active tool, script, or build process? If not → ORPHAN.

DOCUMENT references:
- Every .md, .txt, .pdf: is it linked from active UI, required by a process, or part of a legal/compliance obligation? If not → ORPHAN.

FOLDER references:
- A folder is alive only if it contains at least one ALIVE file.
- Empty folders → always ORPHAN.
- Folders where all contents are ORPHAN → folder itself is ORPHAN.

DUPLICATE DETECTION:
- Identify files with identical or near-identical names that suggest old versions: component_v2.js, utils_old.ts, backup_homepage.html, style_FINAL_v3.css, etc. These are almost always dead. Flag and investigate every one.
- Identify files with identical content (same purpose, different path). Keep one, delete the rest.

════════════════════════════════════════
PHASE 2 — DEAD CODE HUNT INSIDE ACTIVE CODE FILES
════════════════════════════════════════
For every code/style file marked KEEP, do a line-by-line pass and list:

1. Unused imports
2. Unused exports
3. Uncalled functions
4. Unrendered components
5. Dead variables and constants
6. Unused TypeScript types and interfaces
7. Orphaned CSS/SCSS classes not present in any live markup
8. Unused environment variables
9. Detached event handlers
10. Unreachable routes or dead API endpoints
11. Commented-out code blocks
12. console.log / debug / TODO referencing removed features

════════════════════════════════════════
PHASE 3 — DELETION MANIFEST
════════════════════════════════════════
Produce a single numbered manifest covering EVERYTHING to delete:

SECTION A — ENTIRE FILES TO DELETE (any type):
| # | Path | Type | Why it is dead | Cascade: files that reference it and need updating |

SECTION B — ENTIRE FOLDERS TO DELETE:
| # | Folder path | Why it is dead (empty / all contents dead) | Any references to this path in code or config |

SECTION C — DEAD CODE INSIDE ACTIVE FILES:
| # | File path | Lines | What it is | Why it is dead | Cascade impact |

This manifest must be EXHAUSTIVE.
Cross-check it against the Phase 0 census: every ORPHAN file and folder must appear in the manifest.
For every file NOT in the manifest, write explicitly why it is being kept.
No silent survivors.

════════════════════════════════════════
PHASE 4 — EXECUTE (leaves first, shared core last)
════════════════════════════════════════
Process the manifest in dependency order.

For each item:
1. State the item number, path, type, and reason for deletion.
2. Check: does deleting this require changes in any other file or config?
   - YES → make every required change RIGHT NOW before moving on.
   - NO → proceed.
3. For modified files, show before/after diff.
4. Verify all affected files are internally consistent.
5. Mark item complete. Move on.

For FOLDER deletions:
- Confirm every file inside has already been deleted or is being deleted in this step.
- Then delete the folder.
- Search for any hardcoded references to this folder path in code, config, or scripts. Fix them.

NEVER move to the next item while any file, folder, or config is in a broken state.
NEVER make a change not listed on the manifest.
NEVER refactor, rename, or restructure. Delete only.

════════════════════════════════════════
PHASE 5 — REGRESSION SWEEP (every 10 deletions)
════════════════════════════════════════
Every 10 manifest items, stop and run a full sweep:

FUNCTIONAL: No broken imports, undefined references, missing props, broken routes, missing components, orphaned handlers.

FILESYSTEM: No config or build script references a path that no longer exists. No import points to a deleted file or folder.

SECURITY — NEVER remove, regardless of what the graph says:
- Auth guards, session checks, JWT validation.
- Authorization, role, and permission checks.
- Input validation and sanitization.
- CSRF protection, rate limiting.
- Encryption and hashing logic.
- Any code controlling access to data.
If any of the above was touched: restore it immediately.

RENDER: Trace every active route. Confirm every page loads, every component mounts, every asset resolves (no 404s on images, fonts, or static files).

If anything is broken: fix it completely before continuing.

════════════════════════════════════════
PHASE 6 — FINAL AUDIT REPORT
════════════════════════════════════════
1. Re-run the full census from Phase 0. Compare before vs after.
2. For every file and folder still present: confirm it is alive by the Phase 0 definition.
3. For every deleted file: confirm it is not referenced anywhere in the remaining project.
4. For every deleted folder: confirm no path reference to it remains in code or config.
5. Run or simulate a full build. Zero errors.
6. Confirm the app loads, all routes work, no blank pages, no 404 assets, no console errors.

Final report:

   DELETED FILES: X total
   - By type: X code, X style, X asset, X data, X document, X config, X script, X other
   - Full list of every deleted file

   DELETED FOLDERS: X total
   - Full list of every deleted folder

   DEAD CODE REMOVED: X lines across X files
   - List files and line counts

   CASCADE FIXES: X files modified
   - List each file and what was updated

   KEPT (non-obvious): list files that look dead but were kept, with exact reason

   TOTALS:
   - Files: before → after
   - Folders: before → after
   - Lines of code: before → after
   - Build: PASS
   - Security regression: NONE
   - Asset 404s: NONE
   - Render regression: NONE

════════════════════════════════════════
ABSOLUTE RULES — NEVER VIOLATE
════════════════════════════════════════
- Open and read EVERY file of EVERY type. No exceptions. No skipping. No batching.
- This includes documents, notes, markdown files, old scripts, backup files, and any file regardless of extension.
- Assume everything is dead until you find concrete, traceable proof it is alive.
- A file is alive only if you find the exact line of live code, config, or process that uses it.
- Folders are dead if empty or if all their contents are dead. Delete the folder too.
- Every deletion must appear on the manifest before execution.
- Every cascade impact must be resolved in the same step as the deletion.
- No speculative changes. No improvements. No "while I'm here" edits. Delete only.
- The project must be fully functional after every single step. No broken intermediate states.
- Security code is immortal. It never gets deleted regardless of the dependency graph.
- When genuinely unsure about a file: mark it AMBIGUOUS, document why, leave it for human review. Do not silently skip it. Do not silently keep it.